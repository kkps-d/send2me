meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: http://localhost:5183/api/auth/login
  body: json
  auth: inherit
}

headers {
  Accept: text/html
}

body:json {
  {
    "username": "zazademon",
    "password": "zazademon"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
