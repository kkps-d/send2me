meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: http://localhost:5183/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "zazademon",
    "password": "zazademon"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
